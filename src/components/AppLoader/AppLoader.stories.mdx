import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs';
import { AppLoader } from '.';

<Meta title="AppLoader" component={AppLoader} />

# AppLoader

<Canvas>
  <Story name="Default">
    {(args) => {
      return (
        <AppLoader
          styles={{
            width: '100%',
            height: '500px',
            borderBottom: '1px solid #e0e0e0',
          }}
        />
      );
    }}
  </Story>
</Canvas>

## Component arguments

<ArgsTable of={AppLoader} />
